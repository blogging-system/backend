import tagTypes from "./tagTypes";
import tagQueries from "./tagQueries";
import tagMutations from "./tagMutations";

export default `
    ${tagTypes}
    ${tagQueries}
    ${tagMutations}        
`;
